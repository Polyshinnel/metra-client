<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="shortcut icon" href="/assets/img/favicon.svg" type="image/svg+xml">
    <link rel="stylesheet" href="/assets/style.css">
    <title>ЛК Метра | Смена пароля</title>
</head>
<body>
<div class="form-wrapper w-full h-full absolute top-0 left-0 flex justify-center items-center">
    <form action="/restore-password" method="post" class="auth flex flex-col justify-center items-center w-72 m-auto" id="change-pass-form">
        <h1 class="text-xl font-semibold mt-5">Смена пароля</h1>
        <p class="mt-2 text-sm text-center">Сменить пароль для {{ mail }}</p>

        <div class="input-block flex flex-col w-72 mt-3 relative">
            <label for="password" class="text-sm font-semibold">Новый пароль</label>
            <input type="password" name="password" id="new-password" class="border border-gray-400 mt-1 h-10 rounded px-3" placeholder="*****" autocomplete="new-password">
            <img src="/assets/img/eye.svg" alt="" class="absolute w-5 right-3 top-8 cursor-pointer display-pass">
            <p class="hint text-xs mt-1 text-red-500 hidden">Пароль должен быть больше 5 символов</p>
        </div>
        <!--/.input-block-->

        <div class="input-block flex flex-col w-72 mt-3 relative">
            <label for="repeat-password" class="text-sm font-semibold">Повторите пароль</label>
            <input type="password" name="repeat-password" id="repeat-password" class="border border-gray-400 mt-1 h-10 rounded px-3" placeholder="*****" autocomplete="new-password">
            <img src="/assets/img/eye.svg" alt="" class="absolute w-5 right-3 top-8 cursor-pointer display-pass">
            <p class="hint text-xs mt-1 text-red-500 hidden">Пароли не совпадают</p>
        </div>
        <!--/.input-block-->

        <input type="text" class="hidden" id="token" name="token" value="{{ token }}">

        <div class="form-flex flex mt-6 justify-center w-full items-center">
            <input type="submit" value="Сменить пароль" class="reset-pass-btn px-4 bg-blue-600 text-white h-9 rounded hover:bg-blue-800 transition-all duration-300 cursor-pointer">
        </div>
    </form>
</div>

<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
<script src="/assets/js/owl-carousel/owl.carousel.min.js"></script>
<script src="/assets/js/main.js"></script>
</body>
</html>